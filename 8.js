 //importtant array method
 //.map method
 const numbers = [1, 2, 3, 4, 5];

const doubledNumbers = numbers.map((number) => number * 2);

console.log(doubledNumbers);
// Output: [2, 4, 6, 8, 10]
// .filter methd
const num = [1, 2, 3, 4, 5];

const evenNumbers = nu.filter((number) => number % 2 === 0);

console.log(evenNumbers);
// output [2,4]